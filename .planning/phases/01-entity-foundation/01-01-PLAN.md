---
phase: 01-entity-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/java/com/bedomain/BedomainApplication.java
  - src/main/java/com/bedomain/config/DatabaseConfig.java
  - src/main/java/com/bedomain/config/RedisConfig.java
  - src/main/java/com/bedomain/config/SecurityConfig.java
  - src/main/java/com/bedomain/config/JpaAuditingConfig.java
  - src/main/java/com/bedomain/security/AuditUserProvider.java
  - src/main/resources/application.yml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Application starts and connects to MySQL database"
    - "Application connects to Redis for caching"
    - "/actuator/health returns UP status (public access)"
    - "All /api/** endpoints require valid JWT token"
    - "User context extracted from JWT for audit trail"
  artifacts:
    - path: "pom.xml"
      provides: "SpringBoot 3.x with all required dependencies"
      contains: "spring-boot-starter-web, spring-boot-starter-data-jpa, spring-boot-starter-data-redis, spring-boot-starter-oauth2-resource-server"
    - path: "src/main/resources/application.yml"
      provides: "MySQL, Redis, Keycloak JWT configuration"
      contains: "datasource, data.redis, security.oauth2.resourceserver.jwt"
    - path: "src/main/java/com/bedomain/config/SecurityConfig.java"
      provides: "JWT authentication filter chain"
      contains: "SecurityFilterChain, permitAll /actuator/**, authenticated /api/**"
    - path: "src/main/java/com/bedomain/config/JpaAuditingConfig.java"
      provides: "Automatic audit field population"
      contains: "@EnableJpaAuditing, AuditorAware"
  key_links:
    - from: "application.yml"
      to: "SecurityConfig"
      via: "spring.security.oauth2.resourceserver.jwt.issuer-uri"
      pattern: "issuer-uri.*Keycloak"
    - from: "SecurityConfig"
      to: "AuditUserProvider"
      via: "SecurityContextHolder integration"
      pattern: "getCurrentAuditor.*SecurityContextHolder"
---

<objective>
Establish core infrastructure: MySQL database, Redis caching, Spring Boot Actuator health endpoints, and Keycloak JWT authentication. This foundation enables all subsequent entity management APIs.
</objective>

<context>
@.planning/phases/01-entity-foundation/01-CONTEXT.md
@.planning/phases/01-entity-foundation/01-RESEARCH.md

**Key constraints from CONTEXT.md:**
- SpringBoot 3.x with Java 21
- Single-module Maven project
- JPA/Hibernate for ORM with MySQL 8.x
- Keycloak JWT via Spring Security OAuth2 Resource Server
- All endpoints require JWT except /actuator/health
- Redis cache-aside pattern with TTL expiration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Maven project with dependencies</name>
  <files>pom.xml</files>
  <action>
    Create pom.xml with SpringBoot 3.2.0 parent, Java 21. Add dependencies:
    - spring-boot-starter-web
    - spring-boot-starter-data-jpa
    - spring-boot-starter-data-redis
    - spring-boot-starter-oauth2-resource-server
    - spring-boot-starter-actuator
    - mysql-connector-j
    - lombok
    - spring-boot-starter-validation
    - jackson-datatype-jsr310
  </action>
  <verify>
    <automated>mvn dependency:tree -Dincludes=*spring* 2>/dev/null | head -20</automated>
  </verify>
  <done>pom.xml exists with all required SpringBoot dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Configure application.yml</name>
  <files>src/main/resources/application.yml</files>
  <action>
    Configure in application.yml:
    - Server port 8080
    - MySQL DataSource: url, username, password (from env vars)
    - JPA: hibernate ddl-auto: update, show-sql: false
    - Redis: host/port from env vars
    - OAuth2 Resource Server: issuer-uri from KEYCLOAK_ISSUER_URI env var
    - Actuator: expose health,info; health show-details: always
  </action>
  <verify>
    <automated>cat src/main/resources/application.yml | grep -E "datasource|redis|security|management" | head -10</automated>
  </verify>
  <done>application.yml contains MySQL, Redis, JWT, and Actuator configuration</done>
</task>

<task type="auto">
  <name>Task 3: Create SecurityConfig with JWT authentication</name>
  <files>src/main/java/com/bedomain/config/SecurityConfig.java</files>
  <action>
    Create SecurityConfig.java:
    - @EnableWebSecurity with @Configuration
    - SecurityFilterChain bean with:
      - csrf disabled
      - requestMatchers("/actuator/health", "/actuator/info").permitAll()
      - anyRequest().authenticated()
      - oauth2ResourceServer with jwt(Customizer.withDefaults())
    This enables JWT validation via Keycloak for all /api/** endpoints
  </action>
  <verify>
    <automated>grep -n "SecurityFilterChain\|permitAll\|authenticated\|oauth2ResourceServer" src/main/java/com/bedomain/config/SecurityConfig.java</verify>
  </verify>
  <done>SecurityConfig permits /actuator/**, requires JWT for /api/**</done>
</task>

<task type="auto">
  <name>Task 4: Create JpaAuditingConfig and AuditUserProvider</name>
  <files>src/main/java/com/bedomain/config/JpaAuditingConfig.java, src/main/java/com/bedomain/security/AuditUserProvider.java</files>
  <action>
    Create JpaAuditingConfig.java with @EnableJpaAuditing(auditorAwareRef = "auditorAware")
    Create AuditUserProvider implements AuditorAware<String>:
    - Extract user from SecurityContextHolder
    - Prefer JWT "preferred_username" claim, fallback to "sub"
    - Return Optional of username for @CreatedBy/@LastModifiedBy
  </action>
  <verify>
    <automated>grep -n "@EnableJpaAuditing\|AuditorAware\|getCurrentAuditor" src/main/java/com/bedomain/config/JpaAuditingConfig.java src/main/java/com/bedomain/security/AuditUserProvider.java</verify>
  </verify>
  <done>JpaAuditing enabled, AuditUserProvider extracts user from JWT</done>
</task>

<task type="auto">
  <name>Task 5: Create RedisConfig for caching</name>
  <files>src/main/java/com/bedomain/config/RedisConfig.java</files>
  <action>
    Create RedisConfig.java:
    - @Configuration class
    - RedisTemplate<String, Object> bean with Jackson2JsonRedisSerializer
    - RedisCacheManager with default TTL 1 hour
    - Cache configurations for entityTypes (1 hour), entityTypeByName (30 min)
  </action>
  <verify>
    <automated>grep -n "RedisTemplate\|RedisCacheManager\|@EnableCaching" src/main/java/com/bedomain/config/RedisConfig.java</verify>
  </verify>
  <done>RedisConfig configures RedisTemplate and RedisCacheManager</done>
</task>

<task type="auto">
  <name>Task 6: Create main application class</name>
  <files>src/main/java/com/bedomain/BedomainApplication.java</files>
  <action>
    Create BedomainApplication.java with @SpringBootApplication
  </action>
  <verify>
    <automated>grep -n "@SpringBootApplication" src/main/java/com/bedomain/BedomainApplication.java</verify>
  </verify>
  <done>SpringBoot application class exists</done>
</task>

</tasks>

<verification>
- [ ] Application compiles without errors: mvn compile
- [ ] GET /actuator/health returns {"status":"UP"} (no JWT required)
- [ ] GET /api/v1/entity-types without JWT returns 401
- [ ] Redis connection can be established
- [ ] MySQL connection can be established
</verification>

<success_criteria>
Application successfully connects to MySQL and Redis, exposes public health endpoint, and requires valid JWT for all API endpoints. User context is extractable from JWT for audit fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-foundation/01-01-SUMMARY.md`
</output>
