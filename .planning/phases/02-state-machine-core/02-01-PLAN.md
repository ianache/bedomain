---
phase: 02-state-machine-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SM-01, SM-02, SM-03, SM-04, SM-05, SM-06]
user_setup: []

must_haves:
  truths:
    - User can create state machine with states and transitions
    - User can list all state machines
    - User can get state machine by ID with full configuration
    - User can update state machine configuration
  artifacts:
    - path: "bedomain/src/main/java/com/bedomain/domain/entity/StateMachine.java"
      provides: "State machine definition entity"
      contains: "@Entity"
    - path: "bedomain/src/main/java/com/bedomain/domain/entity/StateSpec.java"
      provides: "State specification entity"
      contains: "@Entity"
    - path: "bedomain/src/main/java/com/bedomain/domain/entity/TransitionSpec.java"
      provides: "Transition specification entity"
      contains: "@Entity"
    - path: "bedomain/src/main/java/com/bedomain/repository/StateMachineRepository.java"
      provides: "State machine CRUD repository"
      exports: ["findByEntityTypeId", "findByEntityTypeIdAndDeletedFalse"]
    - path: "bedomain/src/main/java/com/bedomain/service/StateMachineService.java"
      provides: "State machine CRUD operations"
      exports: ["create", "findAll", "findById", "update"]
    - path: "bedomain/src/main/java/com/bedomain/controller/StateMachineController.java"
      provides: "REST API for state machine management"
      exports: ["POST", "GET /", "GET /{id}", "PUT /{id}"]
  key_links:
    - from: "StateMachine.java"
      to: "EntityType.java"
      via: "@ManyToOne entityType"
      pattern: "StateMachine belongs to EntityType"
    - from: "StateMachineController.java"
      to: "StateMachineService.java"
      via: "dependency injection"
      pattern: "StateMachineService"
---

<objective>
Implement state machine configuration CRUD APIs (SM-01 to SM-06).
</objective>

<execution_context>
@C:/Users/ianache/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-machine-core/02-RESEARCH.md

Patterns from Phase 1 (bedomain/src/main/java/com/bedomain/):
- EntityType.java: Entity pattern with soft delete
- EntityTypeService.java: Service layer pattern with @Transactional, CacheEvict
- EntityTypeController.java: REST controller with CRUD endpoints

Key conventions:
- Lombok @Getter/@Setter/@Builder
- JPA with UUID generation
- Soft delete with deleted flag
- DTOs in domain/dto/{entity}/
- Request DTOs use Optional fields for update
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create State Machine Entities</name>
  <files>bedomain/src/main/java/com/bedomain/domain/entity/StateMachine.java
bedomain/src/main/java/com/bedomain/domain/entity/StateSpec.java
bedomain/src/main/java/com/bedomain/domain/entity/TransitionSpec.java</files>
  <action>
Create three JPA entities following Phase 1 patterns:

1. **StateMachine.java** - Main entity:
   - UUID id, String name, String description
   - @ManyToOne EntityType entityType
   - @OneToMany states (cascade ALL, orphanRemoval)
   - @OneToMany transitions (cascade ALL, orphanRemoval)
   - deleted flag, createdAt/createdBy, updatedAt/updatedBy
   - Use @Builder.Default for collections

2. **StateSpec.java** - State definitions:
   - UUID id, String name, String description
   - @ManyToOne StateMachine stateMachine
   - @Enumerated for state type (INITIAL, FINAL, INTERMEDIATE)

3. **TransitionSpec.java** - Transition definitions:
   - UUID id, String event, String description
   - @ManyToOne StateMachine stateMachine
   - @ManyToOne StateSpec fromState
   - @ManyToOne StateSpec toState

All entities use soft delete pattern from Phase 1.
  </action>
  <verify>
mvn compile -q 2>&1 | head -20
</verify>
  <done>Three entity files compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create State Machine DTOs</name>
  <files>bedomain/src/main/java/com/bedomain/domain/dto/statemachine/CreateStateMachineRequest.java
bedomain/src/main/java/com/bedomain/domain/dto/statemachine/UpdateStateMachineRequest.java
bedomain/src/main/java/com/bedomain/domain/dto/statemachine/StateMachineResponse.java
bedomain/src/main/java/com/bedomain/domain/dto/statemachine/StateSpecResponse.java
bedomain/src/main/java/com/bedomain/domain/dto/statemachine/TransitionSpecResponse.java</files>
  <action>
Create DTOs in bedomain/src/main/java/com/bedomain/domain/dto/statemachine/:

1. **CreateStateMachineRequest.java** - Create request:
   - String name, String description
   - UUID entityTypeId
   - List<CreateStateSpecRequest> states
   - List<CreateTransitionSpecRequest> transitions (with fromStateName, toStateName)

2. **UpdateStateMachineRequest.java** - Update request:
   - Optional<String> name, description
   - Add/remove states and transitions via nested objects

3. **StateMachineResponse.java** - Full response with nested states/transitions

4. **StateSpecResponse.java** - State details

5. **TransitionSpecResponse.java** - Transition details with from/to state names

Follow Phase 1 DTO patterns: Lombok @Builder, Jackson annotations for serialization.
  </action>
  <verify>
mvn compile -q 2>&1 | head -20
</verify>
  <done>DTOs compile without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create State Machine Repository & Service</name>
  <files>bedomain/src/main/java/com/bedomain/repository/StateMachineRepository.java
bedomain/src/main/java/com/bedomain/repository/StateSpecRepository.java
bedomain/src/main/java/com/bedomain/repository/TransitionSpecRepository.java
bedomain/src/main/java/com/bedomain/service/StateMachineService.java</files>
  <action>
Create repositories and service:

1. **StateMachineRepository.java**:
   - findByEntityTypeIdAndDeletedFalse(EntityType)
   - findByEntityTypeIdAndDeletedFalsePageable
   - existsByEntityTypeIdAndDeletedFalse

2. **StateSpecRepository.java** - Basic CRUD

3. **TransitionSpecRepository.java** - Basic CRUD

4. **StateMachineService.java**:
   - create(CreateStateMachineRequest): validates entityType exists, builds nested states/transitions, saves
   - findAll(Pageable): paginated list
   - findById(UUID): single SM with full config
   - update(UUID, UpdateStateMachineRequest): modify config
   - Uses JwtAuthenticationService for audit fields
   - Uses @Transactional, @CacheEvict patterns from Phase 1
  </action>
  <verify>
mvn compile -q 2>&1 | head -20
</verify>
  <done>Repository and service compile without errors</done>
</task>

<task type="auto">
  <name>Task 4: Create State Machine Controller</name>
  <files>bedomain/src/main/java/com/bedomain/controller/StateMachineController.java</files>
  <action>
Create REST controller:

**StateMachineController.java**:
- POST /api/v1/state-machines - Create SM
- GET /api/v1/state-machines - List all (paginated)
- GET /api/v1/state-machines/{id} - Get by ID
- PUT /api/v1/state-machines/{id} - Update SM

Follow Phase 1 controller patterns:
- @RestController, @RequestMapping("/api/v1/state-machines")
- @RequiredArgsConstructor
- @Valid on request bodies
- ResponseEntity for responses
- Pageable for list endpoints
  </action>
  <verify>
mvn compile -q 2>&1 | head -20
</verify>
  <done>Controller compiles without errors</done>
</task>

<task type="auto">
  <name>Task 5: Create Unit Tests</name>
  <files>bedomain/src/test/java/com/bedomain/service/StateMachineServiceTest.java
bedomain/src/test/java/com/bedomain/controller/StateMachineControllerTest.java</files>
  <action>
Create unit tests following Phase 1 patterns (JUnit 5 + Mockito):

1. **StateMachineServiceTest.java**:
   - testCreate_Success
   - testCreate_DuplicateName_ThrowsException
   - testFindAll_ReturnsPage
   - testFindById_NotFound_ThrowsException
   - testUpdate_Success
   - Mock EntityTypeRepository, StateMachineRepository, JwtAuthenticationService

2. **StateMachineControllerTest.java**:
   - testCreate_ReturnsCreated
   - testFindAll_ReturnsOk
   - testFindById_ReturnsOk
   - testUpdate_ReturnsOk
   - Mock StateMachineService
   - Use @WebMvcTest
  </action>
  <verify>
mvn test -Dtest="StateMachineServiceTest,StateMachineControllerTest" -q 2>&1 | tail -20
</verify>
  <done>Tests pass with 80%+ coverage</done>
</task>

</tasks>

<verification>
mvn test -q 2>&1 | tail -10
</verification>

<success_criteria>
- [ ] All SM-01 to SM-06 requirements implemented
- [ ] Unit tests pass with 80%+ coverage
- [ ] mvn compile succeeds
- [ ] mvn test passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-machine-core/02-01-SUMMARY.md`
</output>
