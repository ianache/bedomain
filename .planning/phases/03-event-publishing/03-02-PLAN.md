---
phase: 03-event-publishing
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [bedomain/src/main/java/com/bedomain/event/EntityEvent.java, bedomain/src/main/java/com/bedomain/event/EventPublisher.java]
autonomous: true
requirements: [EVNT-02, EVNT-03, EVNT-04]
user_setup: []

must_haves:
  truths:
    - "EventPublisher can create CloudEvents with required fields"
    - "Events include entity ID, type, attributes, timestamp, user"
  artifacts:
    - path: "bedomain/src/main/java/com/bedomain/event/EntityEvent.java"
      provides: "Event data DTO for CloudEvents payload"
      exports: ["EntityEvent", "EntityEventBuilder"]
    - path: "bedomain/src/main/java/com/bedomain/event/EventPublisher.java"
      provides: "Async event publishing to Kafka with CloudEvents"
      exports: ["publishEntityCreated", "publishEntityUpdated", "publishStateChanged"]
---

<objective>
Create the EventPublisher service and event data DTOs using CloudEvents format.
</objective>

<context>
@.planning/phases/03-event-publishing/03-CONTEXT.md
@.planning/phases/03-event-publishing/03-RESEARCH.md
@bedomain/src/main/java/com/bedomain/service/EntityInstanceService.java
@bedomain/src/main/java/com/bedomain/service/StateTransitionService.java

From RESEARCH.md:
- Use CloudEventBuilder from cloudevents-core
- Use KafkaTemplate.send() with CompletableFuture callback for async fire-and-forget
- Topics: bedomain.entities.created, bedomain.entities.updated, bedomain.entities.state-changed
- Event types: com.bedomain.entity.created, com.bedomain.entity.updated, com.bedomain.entity.state-changed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EntityEvent data DTO</name>
  <files>bedomain/src/main/java/com/bedomain/event/EntityEvent.java</files>
  <action>
Create EntityEvent.java in com.bedomain.event package with:
- @Builder, @Data annotations (Lombok)
- Fields: entityId (UUID), entityType (String), attributes (Map<String, Object>), 
  previousState (String, nullable), newState (String, nullable), 
  timestamp (Instant), user (String)
- This is the data payload inside CloudEvent
  </action>
  <verify>
cd bedomain && mvn compile -q
  </verify>
  <done>EntityEvent.java exists with all required fields, project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create EventPublisher service</name>
  <files>bedomain/src/main/java/com/bedomain/event/EventPublisher.java</files>
  <action>
Create EventPublisher.java in com.bedomain.event package:
- @Service, @RequiredArgsConstructor, @Slf4j
- Inject KafkaTemplate<String, String> and ObjectMapper
- Methods:
  - publishEntityCreated(EntityInstance entity): sends to bedomain.entities.created
  - publishEntityUpdated(EntityInstance entity): sends to bedomain.entities.updated  
  - publishStateChanged(EntityInstance entity, String previousState, String newState, String user): sends to bedomain.entities.state-changed
- Each method builds CloudEvent using CloudEventBuilder with:
  - id: UUID.randomUUID().toString()
  - type: "com.bedomain.entity.created|updated|state-changed"
  - source: URI.create("/bedomain")
  - time: Instant.now()
  - dataContentType: "application/json"
  - data: serialized EntityEvent JSON
- Use kafkaTemplate.send() with whenComplete callback for async fire-and-forget
- Log errors, don't throw (log-and-continue per user decision)

Import: io.cloudevents.CloudEvent, io.cloudevents.core.builder.CloudEventBuilder
Import: java.net.URI, java.time.Instant, java.util.UUID
  </action>
  <verify>
cd bedomain && mvn compile -q
  </verify>
  <done>EventPublisher.java exists with 3 publish methods, project compiles</done>
</task>

</tasks>

<verification>
- Maven compile succeeds
- EntityEvent.java contains all required fields (entityId, entityType, attributes, previousState, newState, timestamp, user)
- EventPublisher.java contains publishEntityCreated, publishEntityUpdated, publishStateChanged methods
- Topics used: bedomain.entities.created, bedomain.entities.updated, bedomain.entities.state-changed
</verification>

<success_criteria>
Both tasks complete, Maven build succeeds, EventPublisher ready for integration
</success_criteria>

<output>
After completion, create .planning/phases/03-event-publishing/03-02-SUMMARY.md
</output>
