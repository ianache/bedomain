---
phase: 03-event-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [bedomain/pom.xml, bedomain/src/main/resources/application.yml, bedomain/src/main/java/com/bedomain/config/KafkaConfig.java]
autonomous: true
requirements: [INFRA-03]
user_setup:
  - service: kafka
    why: "Event publishing requires Kafka broker"
    env_vars:
      - name: KAFKA_BOOTSTRAP_SERVERS
        source: "Kafka broker address (e.g., localhost:9092)"

must_haves:
  truths:
    - "Application can connect to Kafka broker"
    - "Topics bedomain.entities.created, bedomain.entities.updated, bedomain.entities.state-changed exist"
  artifacts:
    - path: "bedomain/pom.xml"
      provides: "Kafka and CloudEvents dependencies"
      contains: "spring-kafka"
    - path: "bedomain/src/main/java/com/bedomain/config/KafkaConfig.java"
      provides: "Kafka topic configuration"
      contains: "NewTopic"
    - path: "bedomain/src/main/resources/application.yml"
      provides: "Kafka producer configuration"
      contains: "spring.kafka"
---

<objective>
Set up Kafka infrastructure for event publishing. Add dependencies, configure topics, and set up producer configuration.
</objective>

<context>
@.planning/phases/03-event-publishing/03-CONTEXT.md
@.planning/phases/03-event-publishing/03-RESEARCH.md
@bedomain/pom.xml
@bedomain/src/main/resources/application.yml

From RESEARCH.md - locked decisions:
- Event Format: CloudEvents spec
- Topics: bedomain.entities.created, bedomain.entities.updated, bedomain.entities.state-changed
- Delivery: Fire-and-forget (acks=0)
- Error Handling: Log and continue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Kafka and CloudEvents dependencies to pom.xml</name>
  <files>bedomain/pom.xml</files>
  <action>
Add the following dependencies to pom.xml (after spring-boot-starter-validation):
- spring-kafka 3.2.0
- cloudevents-core 4.0.1
- cloudevents-json-jackson 4.0.1

Add spring-kafka-test with test scope for integration tests.
  </action>
  <verify>
cd bedomain && mvn compile -q
  </verify>
  <done>pom.xml contains spring-kafka and CloudEvents dependencies, project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Add Kafka configuration to application.yml</name>
  <files>bedomain/src/main/resources/application.yml</files>
  <action>
Add Kafka producer configuration under spring.kafka:
- bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
- producer key-serializer: StringSerializer
- producer value-serializer: JsonSerializer
- producer acks: 0 (fire-and-forget)
- producer retries: 0
- producer properties: linger.ms=0, max.block.ms=5000

Add Kafka health indicator under management.health.
  </action>
  <verify>
cd bedomain && mvn compile -q
  </verify>
  <done>application.yml contains Kafka producer config with fire-and-forget settings</done>
</task>

<task type="auto">
  <name>Task 3: Create KafkaConfig.java with topic beans</name>
  <files>bedomain/src/main/java/com/bedomain/config/KafkaConfig.java</files>
  <action>
Create KafkaConfig.java in com.bedomain.config with:
- @Configuration class
- NewTopic beans for:
  - bedomain.entities.created (3 partitions, 1 replica)
  - bedomain.entities.updated (3 partitions, 1 replica)
  - bedomain.entities.state-changed (3 partitions, 1 replica)
- Use TopicBuilder from Spring Kafka

Import: org.apache.kafka.clients.admin.NewTopic, org.springframework.kafka.config.TopicBuilder
  </action>
  <verify>
cd bedomain && mvn compile -q
  </verify>
  <done>KafkaConfig.java exists with 3 NewTopic beans, project compiles</done>
</task>

</tasks>

<verification>
- Maven compile succeeds
- pom.xml contains spring-kafka, cloudevents-core, cloudevents-json-jackson
- application.yml contains spring.kafka configuration
- KafkaConfig.java contains 3 topic beans
</verification>

<success_criteria>
All 3 tasks complete, Maven build succeeds, Kafka infrastructure ready for event publishing
</success_criteria>

<output>
After completion, create .planning/phases/03-event-publishing/03-01-SUMMARY.md
</output>
